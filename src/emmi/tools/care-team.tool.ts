export async function createCareTeamTask(input: {
  patientId: string;
  type: string;
  reason: string;
}) {
  // Create a controlled task in the care-team workflow.
  throw new Error("Implement createCareTeamTask");
}
